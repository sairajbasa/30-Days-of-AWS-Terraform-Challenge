## What is a Variable?

A variable is used to store a value that can be reused multiple times.

Why variables are needed:
- Avoid hardcoding values
- Reduce repetition
- Make code flexible
- Easy environment changes (dev, stage, prod)

Instead of repeating values like `dev` everywhere, we define it once and reuse it.
